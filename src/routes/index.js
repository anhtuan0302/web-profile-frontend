import React from "react";
import { Route, Routes, Navigate } from 'react-router-dom';

const IndexRoutes = () => {
    return (
        <Routes>
        </Routes>
    );
};

export default IndexRoutes;